<%= header %>
<h1>Blorgh install successful!</h1>
<h2>You expected more? Too bad!</h2>

<p>
  <strong>Now we really recommend you run <code>rake install:clean</code> to clean up all the installation files.</strong>
</p>

<p>
  After that, you're free to <a href='/posts'>continue</a>.
</p>
<%= footer %>